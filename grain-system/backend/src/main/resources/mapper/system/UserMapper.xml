<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.grain.system.module.system.mapper.UserMapper">

    <resultMap id="UserVOMap" type="com.grain.system.module.system.vo.UserVO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="last_login_ip" property="lastLoginIp"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectUserPage" resultMap="UserVOMap">
        SELECT u.id, u.username, u.real_name, u.phone, u.email, u.status,
               u.last_login_time, u.last_login_ip, u.create_time
        FROM t_user u
        <if test="roleId != null">
            INNER JOIN t_user_role ur ON u.id = ur.user_id AND ur.role_id = #{roleId}
        </if>
        WHERE u.is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (u.username LIKE CONCAT('%', #{keyword}, '%')
                 OR u.real_name LIKE CONCAT('%', #{keyword}, '%')
                 OR u.phone LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND u.status = #{status}
        </if>
        ORDER BY u.create_time DESC
    </select>

</mapper>
